<UserControl x:Class="AgendaNovo.Controles.HistoricoUsuario"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:conv="clr-namespace:AgendaNovo.Converters"
             xmlns:local="clr-namespace:AgendaNovo.Controles" xmlns:viewmodels="clr-namespace:AgendaNovo.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:CalendarioViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!-- ================== RESOURCES ================== -->
    <UserControl.Resources>
        <!-- Cores/tokens -->
        <SolidColorBrush x:Key="CardBg" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="CardBorder" Color="#D6D9DF"/>
        <CornerRadius   x:Key="CardRadius">12</CornerRadius>
       

        <!-- Chip base -->
        <Style x:Key="ChipStyle" TargetType="Border">
            <Setter Property="Padding" Value="2,0"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Background" Value="#ECEFF1"/>
            <Setter Property="BorderBrush" Value="#CFD8DC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="FotosChip" TargetType="Border" BasedOn="{StaticResource ChipStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Agendamento.Fotos}" Value="Entregue">
                    <Setter Property="Background" Value="#E8F5E9"/>
                    <Setter Property="BorderBrush" Value="#4CAF50"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Agendamento.Fotos}" Value="Revelado">
                    <Setter Property="Background" Value="#FFEBEE"/>
                    <Setter Property="BorderBrush" Value="Yellow"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Chip de Status com cores por estado -->
        <Style x:Key="StatusChip" TargetType="Border" BasedOn="{StaticResource ChipStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Agendamento.Status}" Value="Pendente">
                    <Setter Property="Background" Value="#FFF8E1"/>
                    <Setter Property="BorderBrush" Value="#FFC107"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Agendamento.Status}" Value="Concluido">
                    <Setter Property="Background" Value="#E8F5E9"/>
                    <Setter Property="BorderBrush" Value="#4CAF50"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Agendamento.Status}" Value="Cancelado">
                    <Setter Property="Background" Value="#FFEBEE"/>
                    <Setter Property="BorderBrush" Value="#E53935"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Card do histórico -->
        <Style x:Key="HistoryCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="{StaticResource CardRadius}"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding EstaSendoEditado}" Value="True">
                    <Setter Property="BorderBrush" Value="#2196F3"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Trilho colorido à esquerda, por status -->
        <Style x:Key="StatusStripe" TargetType="Border">
            <Setter Property="Width" Value="4"/>
            <Setter Property="CornerRadius" Value="3"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Background" Value="#B0BEC5"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Agendamento.Status}" Value="Pendente">
                    <Setter Property="Background" Value="#FFC107"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Agendamento.Status}" Value="Concluido">
                    <Setter Property="Background" Value="#4CAF50"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Agendamento.Status}" Value="Cancelado">
                    <Setter Property="Background" Value="#E53935"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    
    
    <Grid Background="#333">
        <ScrollViewer VerticalScrollBarVisibility="Visible">
            <Grid HorizontalAlignment="Center" MaxWidth="1280" Margin="10">
                <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>


            <TextBlock Text="Histórico de Agendamentos"
           FontSize="18" FontWeight="Bold" Foreground="White"
           Margin="10" Grid.Row="0"/>

            <!-- Lista (substitui ScrollViewer + ItemsControl) -->
            <!-- ================== LISTBOX ================== -->
            <ListBox Grid.Row="1"
         ItemsSource="{Binding HistoricoAgendamentos}"
         Background="Transparent"
         BorderThickness="0"
         ScrollViewer.VerticalScrollBarVisibility="Auto"
         VirtualizingPanel.IsVirtualizing="True" Height="1200"
         VirtualizingPanel.VirtualizationMode="Recycling"
         ScrollViewer.CanContentScroll="True">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="VerticalContentAlignment"   Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin"  Value="0"/>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <!-- Clique em qualquer lugar do card -->
                        <Border MouseUp="HistoricoItem_Click"
                                  Padding="0" Background="Transparent" BorderThickness="0"
                                  Cursor="Hand" HorizontalAlignment="Stretch">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- trilho de status -->
                                <Border Grid.Column="0" Style="{StaticResource StatusStripe}"/>

                                <!-- card -->
                                <Border Grid.Column="1" Style="{StaticResource HistoryCard}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <!-- linha 1: data / hora -->
                                        <Grid Grid.Row="0">
                                            <TextBlock Text="{Binding Agendamento.Data, StringFormat='{}{0:dd/MM/yyyy}'}"
                                                FontWeight="SemiBold" />
                                            <TextBlock FontWeight="SemiBold" Text="{Binding Agendamento.Horario, StringFormat='{}{0:hh\\:mm}'}"
                                                HorizontalAlignment="Center" />
                                        </Grid>

                                        <!-- linha 2: chips -->
                                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,6,0,0">
                                            <!-- Mês -->
                                            <Border Style="{StaticResource ChipStyle}"
                                            Visibility="{Binding NumeroMes, Converter={StaticResource NullToVisibilityConverter}}">
                                                <TextBlock Text="{Binding NumeroMes, StringFormat=' Mês {0} '}" />
                                            </Border>

                                            <!-- Status -->
                                            <Border Style="{StaticResource StatusChip}">
                                                <TextBlock Text="{Binding Agendamento.Status}" />
                                            </Border>

                                            <!-- Fotos -->
                                            <Border Style="{StaticResource FotosChip}">
                                                <TextBlock Text="{Binding Agendamento.Fotos, StringFormat=' Fotos: {0} '}" />
                                            </Border>
                                        </StackPanel>

                                        <!-- linha 3: serviço + cliente -->
                                            <StackPanel Grid.Row="2" Orientation="Vertical">
                                                <StackPanel Grid.Row="2" Margin="0,6,0,0">
                                                    <TextBlock Text="{Binding Agendamento.Servico.Nome}"
                                                        FontSize="14"
                                                        FontWeight="SemiBold"/>
                                                </StackPanel>
                                                <StackPanel Grid.Row="3" Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Agendamento.Cliente.Nome}"
                                                        FontSize="14" Margin="0,0,6,0"
                                                        Foreground="#263238" />
                                                    <TextBlock Text="-" FontSize="14" Margin="0,0,6,0"/>
                                                    <TextBlock Text="{Binding Agendamento.Crianca.Nome}"
                                                        FontSize="14"
                                                        Foreground="#263238" />
                                                </StackPanel>
                                            </StackPanel>


                                            <!-- linha 4: pagamento + progressbar -->
                                        <StackPanel Grid.Row="3" Margin="0,8,0,0">
                                            <ProgressBar Height="10"
                             Maximum="{Binding Agendamento.Valor}"
                             Value="{Binding Agendamento.ValorPago, Mode=OneWay}"/>
                                            <TextBlock Margin="0,4,0,0">
                                                      <Run Text="Pago: "/>
                                                      <Run Text="{Binding Agendamento.ValorPago, Mode=OneWay, StringFormat=R$ {0:N2}}"/>
                                                      <Run Text=" / "/>
                                                      <Run Text="{Binding Agendamento.Valor, StringFormat=R$ {0:N2}}"/>
                                            </TextBlock>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
