<Window x:Class="AgendaNovo.Controles.EtapaDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AgendaNovo.Controles"
        mc:Ignorable="d"
        Title="Registrar Etapa" Height="350" Width="400">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,12">
            <TextBlock Text="{Binding Etapa, StringFormat=Etapa: {0}}"
                 FontWeight="Bold" FontSize="16"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Data:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <DatePicker SelectedDate="{Binding DataConclusao}" />
        </StackPanel>

        <StackPanel HorizontalAlignment="Left" Grid.Row="2" Orientation="Vertical">
            <TextBlock Text="Anotações:" Margin="0,0,0,6"/>
            <TextBox Text="{Binding Observacao}" Width="340" Height="120" VerticalContentAlignment="Top"
               TextWrapping="Wrap" AcceptsReturn="True"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
                <Button Width="90" Margin="0,0,8,0" Content="Cancelar" Background="LightGray" IsCancel="True"/>
                <Button Width="90" Content="Salvar" Foreground="White" IsDefault="True" Background="Green"
                Click="Salvar_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
