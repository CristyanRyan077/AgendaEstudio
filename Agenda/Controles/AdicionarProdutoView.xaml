<UserControl x:Class="AgendaNovo.Controles.AdicionarProdutoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AgendaNovo.Controles" xmlns:viewmodels="clr-namespace:AgendaNovo.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:AdicionarProdutoVM}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Width="520">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="16"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Adicionar produto ao agendamento"
                   FontWeight="Bold" FontSize="16" Margin="0,0,0,8"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
                <TextBlock Text="Produto:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <ComboBox Width="280"
                      ItemsSource="{Binding Produtos}"
                      DisplayMemberPath="Nome"
                      SelectedItem="{Binding ProdutoSelecionado, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,8,0,0">
                <StackPanel Width="120">
                    <TextBlock Text="Quantidade"/>
                    <TextBox Text="{Binding Quantidade, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
                <StackPanel Width="140">
                    <TextBlock Text="Valor unit."/>
                <TextBox Text="{Binding ValorUnitario, StringFormat={}{0:N2},UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
                <StackPanel Width="140">
                    <TextBlock Text="Total"/>
                    <TextBox IsReadOnly="True"
                         Text="{Binding Total, Mode=OneWay, StringFormat={}{0:N2}, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancelar"
                    Command="{Binding FecharCommand}" />
                <Button Content="Adicionar"
                    Background="#2E7D32" Foreground="White"
                    Command="{Binding AdicionarCommand}" />
            </StackPanel>
        </Grid>

</UserControl>
