<Window x:Class="AgendaWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:AgendaWPF.ViewModels"   xmlns:hc="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d" Background="LightBlue"  d:DataContext="{d:DesignInstance Type=vm:MainViewModel, IsDesignTimeCreatable=True}"
        Title="MainWindow">
    <Grid>
        <DockPanel>
            <!-- Topo fixo -->
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
                <Button Content="Agenda" Command="{Binding IrParaSemanalCommand}"/>
                <Button Content="Clientes" Command="{Binding IrParaClientesCommand}"/>
            </StackPanel>
        </DockPanel>

    </Grid>
</Window>
